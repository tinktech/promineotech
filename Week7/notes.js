/* Arrow Functions

-Syntax

() => expression

param => expression

let myConciseArrowFunction = parameter1 => expression;

(param) => expression

(param1, paramN) => expression

() => {
  statements
}

param => {
  statements
}

(param1, paramN) => {
  statements
}

let myBlockArrowFunction = (parameter1, parameter2) => { 
    statement; 
    statement; 
    return expression;
};

let sum = (a, b) => a + b;

    let addToPrint = (num1) => {
    let sum = 0;              
    for(let i = 0; i < num1; i++) {
        sum += num1;
    }
    return sum;
};

console.log(addToPrint(4)); // Output: 16

*/

